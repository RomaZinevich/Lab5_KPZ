using Lightweight;

public class Program
{
    static void Main(string[] args)
    {
        CreateHTML createHTML = new CreateHTML();

        createHTML.BuildConstract("div");


        //Console.WriteLine("Total elements: " + elementCount);
    }
   
}